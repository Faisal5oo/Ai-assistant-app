import { PomodoroWorkstation } from "@/components/pomodoro/PomodoroWorkstation";

export const metadata = {
  title: "Pomodoro Focus — TaskFlow",
  description: "Ultra-premium focus timer with deep-work ambient environment.",
};

export default function PomodoroPage() {
  return <PomodoroWorkstation />;
}
